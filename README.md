# MyApp – PySide6 webcam + plugins IA (base)

## Installation (dev)
```bash
python -3.10.6 -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate.bat)
pip install -e .                                     # depuis la racine `mon_projet`
```

## Lancement
```bash
myapp
```